<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>VistaRail - Confirm Ticket</title>
  <style>
    :root {
      --background: #183D3D;
      --text: #DDE6ED;
      --accent: #93B1A6;
    }

    body {
      margin: 0;
      display: flex;
      justify-content: space-evenly;
      align-items: center;
      flex-direction: column;
      height: 100vh;
      font-family: Arial, sans-serif;
      background-color: var(--background);
      color: var(--text);
    }

    .ticket {
      background-color: var(--accent);
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 80%;
      max-width: 600px;
    }

    .ticket-title {
      text-align: center;
      margin-bottom: 20px;
    }

    .ticket-title h1 {
      color: var(--background);
    }

    .ticket-details {
      display: flex;
      flex-direction: column;
    }

    .detail-item {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
    }

    .detail-item span {
      flex: 1;
      text-align: left;
    }

    .detail-item span:first-child {
      margin-right: 10px;
      font-weight: bold;
      color: var(--background);
    }
    .confirm-button {
      text-align: center;
      margin-top: 20px;
    }

    .confirm-button a {
      text-decoration: none;
      color: var(--text);
    }

    .confirm-button input[type="submit"] {
      background-color: var(--accent);
      color: var(--background);
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .confirm-button input[type="submit"]:hover {
      background-color: #567E7E;
    }
  </style>
</head>

<body>
  <div class="ticket">
    <div class="ticket-title">
      <h1>Confirm Your Booking</h1>
    </div>
    <div class="ticket-details">
      <div class="detail-item">
        <span>Train No:</span>
        <span>
          <%= ticket.TrainNumber %>
        </span>
      </div>
      <div class="detail-item">
        <span>Train Name:</span>
        <span>
          <%= ticket.TrainName %>
        </span>
      </div>
      <div class="detail-item">
        <span>Class:</span>
        <span>
          <%= ticket.Class %>
        </span>
      </div>
      <div class="detail-item">
        <span>Journey Date:</span>
        <span>
          <%= ticket.DateOfJourney %>
        </span>
      </div>
      <div class="detail-item">
        <span>Origin:</span>
        <span>
          <%= ticket.Origin %>
        </span>
      </div>
      <div class="detail-item">
        <span>Destination:</span>
        <span>
          <%= ticket.Destination %>
        </span>
      </div>
      <div class="detail-item">
        <span>Departure Time:</span>
        <span>
          <%= ticket.Departure %>
        </span>
      </div>
      <div class="detail-item">
        <span>Arrival Time:</span>
        <span>
          <%= ticket.Arrival %>
        </span>
      </div>
      <div class="detail-item">
        <span>Total Fare:</span>
        <span>₹<%= ticket.Fare %></span>
      </div>
    </div>
  </div>
  <div class="confirm-button">
    <a href="/ticketConfirmation"><input type="submit" value="Pay and Confirm" /></a>
  </div>
</body>

</html>